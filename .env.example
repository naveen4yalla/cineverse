# Optional: enable live TMDB mode.
# Get a free API key (v3 auth) at https://www.themoviedb.org/settings/api
# When unset, Cineverse runs entirely on its embedded demo dataset — no key required.
VITE_TMDB_API_KEY=your_tmdb_v3_api_key_here
